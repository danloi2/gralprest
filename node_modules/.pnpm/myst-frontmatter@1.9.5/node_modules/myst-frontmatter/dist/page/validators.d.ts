import type { ValidationOptions } from 'simple-validators';
import { type PageFrontmatter } from './types.js';
/**
 * Frontmatter keys that inherit from project to page when not set on the page.
 * Add new keys here if you want them to propagate from project-level config to pages.
 */
export declare const USE_PROJECT_FALLBACK: string[];
export declare function validatePageFrontmatterKeys(value: Record<string, any>, opts: ValidationOptions): PageFrontmatter;
/**
 * Validate single PageFrontmatter object against the schema
 */
export declare function validatePageFrontmatter(input: any, opts: ValidationOptions): PageFrontmatter;
//# sourceMappingURL=validators.d.ts.map