import type { ValidationOptions } from 'simple-validators';
import type { PageFrontmatter } from '../page/types.js';
import type { ProjectFrontmatter } from '../project/types.js';
import type { SiteFrontmatter } from '../site/types.js';
/**
 * Fill missing values from page frontmatter object with values from project frontmatter
 *
 * This only applies to frontmatter values where overriding is the correct behavior.
 * For example, if page has no 'title' the project 'title' is not filled in.
 */
export declare function fillPageFrontmatter(pageFrontmatter: PageFrontmatter, projectFrontmatter: ProjectFrontmatter, opts: ValidationOptions): PageFrontmatter;
/**
 * Fill missing values in `base` frontmatter with defaults from `filler`.
 * Used to inherit project-level settings into pages.
 */
export declare function fillSiteFrontmatter(base: SiteFrontmatter, filler: SiteFrontmatter, opts: ValidationOptions, keys?: string[], trimUnused?: boolean): SiteFrontmatter;
export declare function fillProjectFrontmatter(base: ProjectFrontmatter, filler: ProjectFrontmatter, opts: ValidationOptions, keys?: string[], trimUnused?: boolean): ProjectFrontmatter;
//# sourceMappingURL=fillPageFrontmatter.d.ts.map