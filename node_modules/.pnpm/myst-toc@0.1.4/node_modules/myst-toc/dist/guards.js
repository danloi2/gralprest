export function isFile(entry) {
    return entry.file !== undefined;
}
export function isURL(entry) {
    return entry.url !== undefined;
}
export function isPattern(entry) {
    return entry.pattern !== undefined;
}
